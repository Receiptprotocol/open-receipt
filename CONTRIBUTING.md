# Contributing

Open an issue before proposing a wire-format change. Specification changes must
include schema changes, positive and negative public vectors, verifier tests,
and an explicit compatibility note.

Run `bun install --frozen-lockfile` and `bun run check` before opening a pull
request. Never commit signing keys, credentials, private evidence, or generated
dependency directories.
